{
  "name": "integradata_web",
  "version": "1.0.0",
  "private": true,
  "description": "Landing de Integradata con enfoque de alto impacto visual.",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "astro build && firebase deploy --only hosting"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "astro": "^5.7.2"
  }
}
